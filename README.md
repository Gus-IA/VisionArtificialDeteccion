# Detección de Objetos con PyTorch y Faster R-CNN

Este proyecto demuestra cómo usar el modelo **Faster R-CNN** de `torchvision` para realizar detección de objetos sobre el dataset **Pascal VOC**.  
Se incluyen funciones para visualizar imágenes, anotaciones y predicciones.

---

## 🚀 Características
- Descarga automática del dataset **VOCDetection** de `torchvision`.
- Visualización de imágenes con sus **bounding boxes** y etiquetas.
- Uso de un modelo **Faster R-CNN con ResNet-50 FPN** preentrenado en COCO.
- Función de predicción personalizada con umbral ajustable.
- Ejemplos visuales comparando anotaciones reales y detecciones del modelo.

---

🧩 Requisitos

Antes de ejecutar el script, instala las dependencias:

pip install -r requirements.txt

🧑‍💻 Autor

Desarrollado por Gus como parte de su aprendizaje en Python e IA.
